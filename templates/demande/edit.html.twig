{% extends 'base.html.twig' %}

{% block title %}Edit Demande{% endblock %}

{% block content %}

  <div class="container py-5">
    <div class="row">
      <div class="col-lg-8">
        <div class="card mb-4">
            <div class="card-body">
              <h1>Modifier Demande N°{{ demande.id }}</h1>
              <div class="row">
                  {{ include('demande/_form.html.twig', {'button_label': 'Modifier'}) }}
                 {{ include('demande/_delete_form.html.twig') }}
         </div>
        </div>
      </div>
    </div>
  </div>

{% endblock %}
