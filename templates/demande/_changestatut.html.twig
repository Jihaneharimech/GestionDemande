
        {{ form_start(form) }}
         <div class="row" style="display: flex;align-items: center;">
            <div class="col-8 statut-form" >
            {{ form_row(form.statut ) }}
            </div>
            <div class="col-2" style="margin: -9% 0 0 -7%;">
            <button class="btn btncheck"><i class="fa-solid fa-check"></i>
                {{ button_label|default('') }}</button>        
            </div>
            </div>
       {{ form_end(form) }} 

       